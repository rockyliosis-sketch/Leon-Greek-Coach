# A 段教材重建 —— 进度与方法（断点续做用）

> 最后更新：2026-09-02
> **红线：本次重建绝不编造任何一个词。提不出来就如实标注缺口，不用占位符填充。**

---

## 一、为什么要重建

原 AI 工具产出的三份 Markdown 教材（A1-A / A1-B / A2）**是编造的**，证据：

1. 这三个 PDF 是手机拍的照片，**没有文字层**，任何工具都不可能"读"出文字。
2. 三本书的照片**页序全是反的**（从后往前拍），而 Markdown 按正序编排 —— 说明它根本没看过图。
3. 5 处随机抽查，**5 处全部对不上**原书内容。

B 本（`Ελληνικά Β.pdf`）不同：它有文字层，只是用了 2012 年的旧字形编码。已解码，**385 页比对中位准确率 97.4%，是真的**。

---

## 二、已建成的基础设施

| 资产 | 路径 | 说明 |
|---|---|---|
| 希腊语识别包 | tesseract `ell` + `grc` | 来自官方 `tessdata_best`（最高精度版） |
| **权威词典** | `materials/glossaries/AUTHORITATIVE_DICT.json` | **3038 条**（A1 1350 + A2 1688），来自希腊教育部官方词汇表 PDF，中英双语 |
| 词典索引 | `materials/glossaries/AUTHORITATIVE_INDEX.json` | 2654 个匹配键 → 规范词条，含 `-άω/-ώ` 双写归一 |
| 词典构建脚本 | `scripts/glossary/build_dict.py` | 可重跑 |
| 词形还原器 | `scripts/ocr/morph.py` | 按希腊语屈折词尾规则还原词干（非编辑距离） |
| OCR 流水线 | `scripts/ocr/greek_ocr.py` | 渲染 300dpi → tesseract → 多调转单调 |
| 封闭词表搜索 | `scripts/ocr/wordfind.py` | **核心方法**，见下 |
| 全量 OCR 批处理 | `scripts/ocr/batch_ocr.py` | **可断点续跑**，已有结果自动跳过 |
| OCR 文本落盘 | `scratch/ocr_text/<book>/pNNN.txt` | 每页一个文件，首行含书页号 |
| **B 本单元映射** | `materials/textbooks/B_unit_page_map.json` | **已完成**，见第五节 |
| B 本解码全文 | `scratch/pdf_verify/B_pdf_decoded.txt` | 394 页，非 OCR，可信 |
| B 本字形解码器 | `scratch/pdf_verify/decode.py` | 旧字形 → 标准 Unicode |
| 核对报告 | `docs/教材PDF与Markdown核对报告_2026-09-02.md` | 造假证据全在这 |

---

## 三、页序公式（已双点验证）

三本 A 段书都是**从后往前拍**，且每个 PDF 页 = 一个跨页（两个书页）。

| 书 | PDF 页数 | PDF 第 N 页 → 原书页码 | 覆盖 |
|---|---:|---|---|
| A1-A | 91 | `10 + (91−N)×2` | 原书 10–191 |
| A1-B | 85 | `6 + (85−N)×2` | 原书 6–174 |
| A2 | 67 | `16 + (67−N)×2` | 原书 16–148 |

---

## 四、核心方法：封闭词表反向搜索

**不要试图"读出整页课文"，只问"词典里的哪些词出现在这一页"。**

前者是识别问题（难，OCR 全文可信率只有 43–54%）；后者是在 3038 词封闭集合里做匹配（容错空间大得多）。

实测（A1-B 原书 96–97 页，"我喜欢运动"单元）：

| 指标 | 数值 |
|---|---:|
| 全文 OCR 可信识别率 | 43–54% |
| **封闭词表反向搜索 · 召回率** | **75–86%** |
| **误报率** | **2.8%**（且其中 `μπαμπάς`、`Άννα` 经查原文确实存在，实际更低） |

容错阈值按词长分级（短词必须近乎精确）：`≤4字→0, ≤6→1, ≤10→2, 更长→3`

---

## 五、B 本（Ελληνικά Β'）—— 单元映射已完成

数据源：解码全文里的**目录**（ΕΝΟΤΗΤΑ n + 页码）+ **InDesign 版式页码标记**（`...:Layout ... Page N`）。
**391/394 页有精确页码锚点**，页数与目录完全吻合，不是估算。

| B单元 | 系统单元号 | 书页 | 标题 |
|---:|---:|---|---|
| 1 | 40 | 8–25 | Αφήστε το μήνυμά σας |
| 2 | 41 | 26–41 | Σπίτι μου σπιτάκι μου |
| 3 | 42 | 42–61 | Είχε τέτοια κίνηση! |
| 4 | 43 | 62–77 | Είναι πανάκριβα! |
| 5 | 44 | 78–83 | Πάμε πάλι!（复习） |
| 6 | 45 | 84–103 | Φάγαμε, ήπιαμε… |
| 7 | 46 | 104–119 | Θυμάμαι ότι παίζαμε όλη μέρα… |
| 8 | 47 | 120–139 | Έχει ο καιρός γυρίσματα |
| 9 | 48 | 140–155 | Αλλάζουμε συνήθειες |
| 10 | 49 | 156–163 | Πάμε πάλι!（复习） |
| 11 | 50 | 164–185 | Πάμε διακοπές; |
| 12 | 51 | 186–203 | Ένα ατύχημα στους δρόμους |
| 13 | 52 | 204–233 | Περιμένετε μισό λεπτό, παρακαλώ |
| 14 | 53 | 234–254 | Σήμερα γιορτάζουμε |
| 15 | 54 | 255–263 | Πάμε πάλι!（复习） |
| 16 | 55 | 264–283 | Παρακολουθώ συχνά το κανάλι σας |
| 17 | 56 | 284–303 | Μάθε, παιδί μου, γράμματα |
| 18 | 57 | 304–325 | Δουλεύω σαν σκυλί… |
| 19 | 58 | 326–345 | Είναι πολύ της κουλτούρας |
| 20 | 59 | 346–356 | Πάμε πάλι!（复习） |

**注意**：B 本没有官方分单元词汇表（附录只有语法表、听力原文、答案）。
B 的真词提取思路：课文里出现、但**不在 A1+A2 词典（3038 词）里**的词 = B 阶段新词。因为 B 本文本是解码的真文本，这一步准确度接近 100%，只是需要人工/模型确认词形原形。

---

## 六、已否决的两条路（别再试）

### 1. 编辑距离自动纠错 —— 危险，已废弃
把 OCR 出的词按最小编辑距离"修"成词典词，结果把**本来对的词改成不相干的词**：
```
Κώστα(人名)  → σωστά(正确的)     ❌
γράφεις(你写) → γραφείο(办公室)   ❌
έχει(他有)    → εκεί(那里)        ❌
```
根因：词典收原形，课文里全是变位形式。已改用 `scripts/ocr/morph.py` 的词尾规则还原。

### 2. 图像预处理 —— 无效
二值化 / 光照均衡 / 4 种 PSM 模式全试过，**可信词数稳定在 67 个左右纹丝不动**，只是噪声多少不同。调参已到顶。

---

## 七、踩过的坑

- **并行 OCR 时临时图片文件名撞车**：三本书的 `p10-10.png` 同名，互相覆盖删除，导致 50 多页 OCR 结果为空。已改成 `{book}_p{page}` 独立命名。**重跑前先 `find scratch/ocr_text -size -100c -delete` 清掉空文件。**
- `/tmp` 下的临时文件子进程读不到（沙箱限制），工作目录必须放项目内 `scratch/ocr_work`。
- tesseract 的 stderr 不是 UTF-8，`subprocess` 要加 `errors='replace'`。
- A1-A 前两页 `Malformed JP2`，pdftoppm 会警告但仍出图，不影响。

---

## 八、下一步（接着干）

1. **[进行中] 全量 OCR** 243 个跨页 → `scratch/ocr_text/`。断了就重跑 `python3 scripts/ocr/batch_ocr.py all`，已完成的自动跳过。
2. **单元边界识别**：靠页面上的 `Ενότητα N` 标记 + 页序公式锚定。A 段三本书的 PDF 都缺目录页（拍摄起点分别是原书 10/6/16 页），所以只能靠正文页眉标记。
3. **每单元真词提取**：对每单元的页面跑封闭词表反向搜索。
4. **人工抽查**：每本抽 2–3 个单元，与实拍图逐词核对后**才**写入数据库。
5. B 本真词提取（见第五节思路）。

## 九、这件事做完之后，还欠用户两件事

- **P1 家长端进度条重构**：把"单元"降级为容器，操作单位改成"这次课讲到哪"。按课本页码把单元切成学习块，家长每次课后拨一下滑块（约 2 秒），艾宾浩斯锚点挂在**块**上而不是单元上，出题只从已推进的块里抽。默认自动前进，家长只在偏离时纠正。
- **P2 出题逻辑重构**：现状是每单元 12 个词硬凑 30 道题（同一个词在 5 种题型里反复出现），且词是按字母顺序切给单元的，跟单元主题无关。要做到：同一个词不跨题型重复；语法/句子类题目占比从 18% 提到 50% 以上。
